{
  "name": "ultimate-offgrid-survival-guide",
  "version": "1.0.0",
  "description": "Complete off-grid survival encyclopedia with 60+ scenarios",
  "scripts": {
    "build": "echo 'Building for production...'",
    "android:setup": "npx cap add android && npx cap sync android",
    "android:build": "npx cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^5.7.0",
    "@capacitor/app": "^5.0.7",
    "@capacitor/core": "^5.7.0",
    "@capacitor/splash-screen": "^5.0.7",
    "@capacitor/status-bar": "^5.0.7"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.7.0"
  }
}
